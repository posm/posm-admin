var omkServerPath = require('../../settings').omkServerPath;
var formsPath = omkServerPath + '/data/forms';

module.exports = function (io, fullDeploy) {
    return function (req, res, next) {

    };
};
